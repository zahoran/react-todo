import React from 'react';
import './Loader.css';

const loader = (props) => {
  return (
    <div className='Overlay'>
      <div className='loader'>Loading...</div>
    </div>
  )
};

export default loader;
